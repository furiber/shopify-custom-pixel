# shopify-custom-pixel
Custom pixel for shopify to insert GTM and push all the GA4 expected events into the dataLayer. It includes setup for server-side GTM allowing you to define it and send the traffic to it, as opposed to just using client-side GTM.
